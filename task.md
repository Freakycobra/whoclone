# Phase 2 Task

## Status: COMPLETE ✅

All items done:
- [x] constants/index.js — 11 regional pricing tiers + INTERESTS + COUNTRIES
- [x] hooks/useRegionalPricing.js — GPS-enforced pricing hook
- [x] store/followStore.js — follow/unfollow with AsyncStorage + backend sync
- [x] CoinStoreScreen.js — uses useRegionalPricing, clean file (no duplication)
- [x] PremiumScreen.js — uses useRegionalPricing, clean file (no duplication)
- [x] backend/src/routes/users.js — POST /follow + POST /unfollow + GET /:id/follow-counts
- [x] ProfileSetupScreen.js — interests picker (pick 3-5)
- [x] ProfileScreen.js — interest tags + followers/following count
- [x] VideoChatScreen.js — Follow button after 10s using useFollowStore
- [x] bonusStore.js — streak counter + streakFreezeCount + buyStreakFreeze
- [x] DailyBonusModal.js — streak flame + freeze button (streak > 2)
- [x] HomeScreen.js — handleSelectGender already has await spendCoins

## Commit target: feat: Phase 2 - regional pricing, interests, follow system, streak
